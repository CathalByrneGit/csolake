# Disconnect from the CSO Data Lake

Disconnect from the CSO Data Lake

## Usage

``` r
db_disconnect()
```

## Value

Invisibly returns TRUE if disconnected, FALSE if was not connected.

## Examples

``` r
if (FALSE) { # \dontrun{
db_connect()
# ... do work ...
db_disconnect()
} # }
```
