# List views in a DuckLake schema

Returns all views in a given schema.

## Usage

``` r
db_list_views(schema = "main")
```

## Arguments

- schema:

  Schema name (default "main")

## Value

Character vector of view names
